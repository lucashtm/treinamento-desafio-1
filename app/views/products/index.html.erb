<h1>CHOOSE ITEMS</h1>
<h2 class="user">Hello, <%= current_user.name %></h2>
<% @products.each do |product|%>
  <div class="product-brief" id="<%= "product-#{product.id}" %>">
    <%= render_product(product) %>
    <%= add_to_cart(product) %>
  </div>
<% end%>